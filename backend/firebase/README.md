# Firebase Admin Credentials Directory

Place your Firebase Service Account JSON credentials file inside this directory.

For example, name it:
`service-account.json`

Then, set the environment variable in `backend/.env`:
`FIREBASE_CREDENTIALS_PATH="firebase/service-account.json"`
