name: shared_api
description: Shared API client with automatic JWT token injection and 401 refresh mechanism.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  flutter_riverpod: ^2.4.9
  socket_io_client: ">=3.0.0 <3.1.0"
